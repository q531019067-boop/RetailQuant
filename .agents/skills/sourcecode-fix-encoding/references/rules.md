# 执行规则

1. 优先确认修复基准：
   - 有可靠备份时优先 `--backup`；
   - 无备份时使用 SVN 模式。
2. 批量修复前先做一次 `--dry-run`，先看命中数量与日志。
3. 只处理用户指定范围；避免默认全仓扫描。
4. 修复后建议抽样检查：
   - 是否仍有 `�`（U+FFFD）；
   - 是否引入新的 `???` 注释；
   - 是否误改非注释代码。
5. 若 `svn` 不可用，必须切换 `--backup` 模式。
6. 对关键目录先做备份或保证可回滚（SVN/本地副本）。
